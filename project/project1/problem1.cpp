#include <iostream>
#include <math.h>

using namespace std;

int main()
{
    long int x,y,w=0,m=0;
    float sum1=0,sum2=0;
    cout<<"Enter the temp you want to cook with \n";
    cin>>x;
    y=x;

  else
            {   //cout<<"yee"<<endl;
                sum1=(s[i]+1)*pow(10,i);
                sum2=(s[i])*pow(10,i)-1;
            }
    }
    else
        {
        sum1+=s[i]*pow(10,i);
        sum2+=s[i]*pow(10,i);
        }
    }
return 0;
}
