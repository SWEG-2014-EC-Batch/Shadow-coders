#include<iostream>
using namespace std;
int main(){
  else
            {   //cout<<"yee"<<endl;
                sum1=(s[i]+1)*pow(10,i);
                sum2=(s[i])*pow(10,i)-1;
            }
    }
    else
        {
        sum1+=s[i]*pow(10,i);
        sum2+=s[i]*pow(10,i);
        }
    }
return 0;
}
